import App from './App';
import React from 'react';
/* eslint-disable no-undef */
import { render } from "@testing-library/react";

describe('App ', () => {
    it('is running...', () => {
        render(<App />);
    });
});
